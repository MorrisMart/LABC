/*
    Creare un programma che:
        a. definisce nel main un array di double grande a piacere (la dimensione dell’array
           deve essere definita con un constexpr o variabile costante);
    b. stampa la dimensione dell’array usando sizeof;
    c. passa l’array come argomento a una funzione print_array_length(),
    opportunamente dichiarata e definita, che a sua volta stampa la dimensione
    dell’array usando sizeof. La funzione print_array_length() conosce la
    dimensione dell’array? Riesce ad accedere ai dati dell’array? L’accesso è lecito?
    Commentare i risultati ottenuti con i vicini di banco.
*/

#include <iostream>

//main 
int main (void)
{

    return 0;
}